[test_pack]
default_remote_control = horizon

[device_under_test]
device_type = horizon

[node]
friendly_name = DCTG, horizon_stb1
# friendly name can be used to identify specific stb. Will see this in the portal and can identify
# a specific stb to use for a test_pack
# use things like location, destination, rack number, shelf number etc....